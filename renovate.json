{
  "extends": ["config:base"],
  "schedule": "every weekend",
  "rangeStrategy": "bump",
  "postUpdateOptions": ["npmDedupe"],
  "packageRules": [
    {
      "packageNames": ["postcss"],
      "rangeStrategy": "replace"
    }
  ]
}
